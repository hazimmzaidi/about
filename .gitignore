# Dependencies — never commit these; they're reinstallable from package.json
node_modules/

# Abandoned Next.js rebuild, kept locally for reference only
portfolio (javascript_backup)/

# Next.js build output
.next/
out/

# Local planning notes, not part of the site
PORTFOLIO_PLAN.md
draft of old blog

# Editor workspace config, not site content
*.code-workspace

# Editor and OS cruft
.DS_Store
Thumbs.db
*.log
